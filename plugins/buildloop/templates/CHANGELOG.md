<!--
CHANGELOG. TEMPLATE (one per repo, at the repo root).
`ship-in-prd` prepends an entry at ship; `tweak-it` adds a line per change.
Newest first. Each line answers WHO changed WHAT (≤280 chars), linked to its
feature-document / fc id. Keep it human-readable; it is the durable change
history that the living feature-documents deliberately do not retain.
-->

# Changelog

All notable changes to this project, newest first.

## [Unreleased]

## <YYYY-MM-DD>: <feature name> (fc-NNNN)

- <what changed, from the user/system perspective> (<who>)
